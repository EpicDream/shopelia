%article#collectionArticle
  .topHome
    .spacer25
    .container
      .row
        .collection-title
          = @collection.name
      .spacer25
      - if @collection.belongs_to?(@current_user)
        .row
          = render "add_product"
        .spacer25
      .row#collection
        = render @collection.items
    .spacer200