= form_for([:admin,:georges,@device,Message.new], remote: true, :html => { :id => 'message_form' }) do |f|
  = f.text_field :content , autofocus: "", placeholder: "Type your message?", type: "text"
  %label.checkbox
    %input.checkbox{type:"checkbox",id:"data-checkbox", checked:false} send product urls
  = f.text_area :products_urls, :rows => 10
  = f.submit "Répondre", :class => 'button-flat-empty '


