%section.header
  %header
    %h1 Messages
    %h2 Talk with users, Make their dream come true

.container-fluid
  .row-fluid
    .span2
      %article.coolbox
        %h3 Filters
    .span10
      %article.coolbox
        %h3 Merchants list
        %table#messages.table.table-striped
          %thead
            %tr
              %th Device Id
              %th Content
              %th Actions
          %tbody
            -  @devices.each do |device|
              - unless device.messages.last.nil?
                %tr
                  %td= device.messages.last.device_id
                  %td= device.messages.last.content
                  %td= link_to "show conversation", admin_device_messages_path(device)